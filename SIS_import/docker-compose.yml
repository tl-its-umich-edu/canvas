version: '3'

services:
  job:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ${HOME}/secrets/sis:/app/config/secrets
    container_name: sis
